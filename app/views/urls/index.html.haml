%table.table
  %thead
    %tr
      %th No.
      %th Original URL
      %th Short URL
  %tbody
    - @urls.each_with_index do |url, index|
      %tr
        %td= index + 1
        %td= url.original
        %td= url.short

= paginate @urls